#include <SFML/Graphics.hpp>
#include <vector>

// 冒泡排序算法
void bubbleSort(std::vector<int>& arr, sf::RenderWindow& window);

void quickSort(std::vector<int>& arr, sf::RenderWindow& window, int left = 0, int right = -1); 
